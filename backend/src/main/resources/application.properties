server.port=8080

# Allow large SQL file uploads (up to 10MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# App info
spring.application.name=db-visualizer